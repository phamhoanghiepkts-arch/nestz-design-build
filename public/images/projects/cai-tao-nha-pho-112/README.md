# Cải tạo nhà phố 112 image placeholders

Replace these files later with real project assets:

- `before-01.jpg` = ảnh hiện trạng mặt tiền trước cải tạo
- `after-01.jpg` = ảnh mặt tiền sau cải tạo
- `process-01.jpg` = ảnh thi công/quá trình
- `render-01.jpg` = ảnh phối cảnh/render nếu có

Do not rename these files unless the paths are also updated in `src/data/images.ts`.
